db.createCollection(usercollection, { pseudo: string,name: string,password: string,admin: boolean } )
db.createCollection(markercollection, { pseudo: string,
                              latitude: number,
                              longitude: number,
                              year: string,
                              place: string,
                              url: string,
                              nblike: number } )
db.createCollection(likecollecion, { pseudo: string,
                              idmarker: string } )